[project]
name = "edu-tutor-system"
version = "0.1.9"
description = "Local-first K12 multi-subject tutoring system"
requires-python = ">=3.11"
dependencies = [
  "streamlit>=1.35",
  "PyYAML>=6.0",
  "Jinja2>=3.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
