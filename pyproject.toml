[project]
name = "math-tutor-system"
version = "0.1.4"
description = "Grade 5 math mistake training and worksheet generation MVP"
requires-python = ">=3.11"
dependencies = [
  "streamlit>=1.35",
  "PyYAML>=6.0",
  "Jinja2>=3.1",
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
