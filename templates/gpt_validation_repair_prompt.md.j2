YAML parsed, but it failed edu_tutor_system v0.1.9-pretrial validation.

source_type: {{ source_type }}

human_readable_validation_report:
```yaml
{{ readable_report_yaml }}
```

raw_validation_report:
```yaml
{{ raw_validation_yaml }}
```

legal_question_type_code:
```yaml
{{ question_types_yaml }}
```

legal_knowledge_point_id:
```yaml
{{ knowledge_points_yaml }}
```

legal_mistake_tag_code_or_target_mistake_tag_code:
```yaml
{{ mistake_tags_yaml }}
```

legal_difficulty_code:
```yaml
{{ difficulties_yaml }}
```

subject_scoped_alias_mappings:
```yaml
{{ alias_mappings_yaml }}
```

Repair rules:
- Only repair fields, enum values, missing required fields, and unsupported fields.
- Do not rewrite the core question content.
- Use canonical fields: question_type_code, knowledge_point_id, primary_mistake_tag_code, target_mistake_tag_code, difficulty_code.
- Keep allowed v0.1.9-pretrial optional fields when valid: diagnosis_confidence, needs_human_review, secondary_mistake_tags, diagnosis_evidence, alternative_diagnoses, primary_target_id, question_role, teaching_purpose, expected_error_mechanism.
- Do not output old free-text fields as the recommended schema.
- Do not invent code/id values outside the registry.
- v0.1.9-pretrial supports plain text only; do not output visuals, render_blocks, diagram, formula_block, chemical_equation_block, or physics_diagram.
- question_role must be repair, variant, transfer, or mixed_review.
- secondary_mistake_tags must contain at most 3 allowed current mistake tags.
- diagnosis_confidence and alternative_diagnoses.confidence must be between 0.0 and 1.0.
- Output YAML only. No Markdown fences.
- mistakes output must start with `mistakes:`.
- worksheet output must start with `worksheet:`.

Current v0.1.9-pretrial mistakes.yaml canonical structure:
```yaml
mistakes:
  - date: "2026-06-03"
    student_id: "daughter"
    subject_id: "math"
    grade_at_time: 6
    term_at_time: "六年级上"
    curriculum_version_at_time: "cn_k12_2022"
    textbook_version_at_time: "沪教版"
    question_type_code: "math_application"
    knowledge_point_id: "math_g6a_percentage_word_problem_model"
    primary_mistake_tag_code: "MATH_QUANTITATIVE_RELATION_ERROR"
    difficulty_code: "medium"
    question_summary: "..."
    wrong_answer_summary: "..."
    correct_answer_summary: "..."
    training_needed: true
    diagnosis_confidence: 0.82
    needs_human_review: false
    secondary_mistake_tags: []
    diagnosis_evidence: {}
    alternative_diagnoses: []
    source: "GPT marking"
```

Current v0.1.9-pretrial worksheet.yaml canonical structure:
```yaml
worksheet:
  title: "六年级数学专项训练"
  date: "2026-06-03"
  student_id: "daughter"
  subject_id: "math"
  grade_at_time: 6
  term_at_time: "六年级上"
  curriculum_version_at_time: "cn_k12_2022"
  textbook_version_at_time: "沪教版"
  sections:
    - name: "一、应用题建模"
      layout: "single_column"
      questions:
        - question_type_code: "math_application"
          knowledge_point_id: "math_g6a_percentage_word_problem_model"
          target_mistake_tag_code: "MATH_QUANTITATIVE_RELATION_ERROR"
          difficulty_code: "medium"
          primary_target_id: "math_g6a_percentage_word_problem_model::MATH_QUANTITATIVE_RELATION_ERROR"
          question_role: "repair"
          teaching_purpose: "..."
          expected_error_mechanism: "..."
          question: "..."
          answer: "..."
          explanation: "..."
```

original_yaml:
```text
{{ original_text }}
```
