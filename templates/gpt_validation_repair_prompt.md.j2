YAML parsed, but it failed edu_tutor_system v0.1.7.3 validation.

source_type: {{ source_type }}

human_readable_validation_report:
```yaml
{{ readable_report_yaml }}
```

raw_validation_report:
```yaml
{{ raw_validation_yaml }}
```

legal_question_type_code:
```yaml
{{ question_types_yaml }}
```

legal_knowledge_point_id:
```yaml
{{ knowledge_points_yaml }}
```

legal_mistake_tag_code_or_target_mistake_tag_code:
```yaml
{{ mistake_tags_yaml }}
```

legal_difficulty_code:
```yaml
{{ difficulties_yaml }}
```

subject_scoped_alias_mappings:
```yaml
{{ alias_mappings_yaml }}
```

Repair rules:
- Only repair fields, enum values, missing required fields, and unsupported fields.
- Do not rewrite the core question content.
- Use canonical fields only: question_type_code, knowledge_point_id, primary_mistake_tag_code, target_mistake_tag_code, difficulty_code.
- Do not output old free-text fields as the recommended schema.
- Do not invent code/id values outside the registry.
- v0.1.7.3 supports plain text only; do not output visuals, render_blocks, diagram, formula_block, chemical_equation_block, or physics_diagram.
- Output YAML only. No Markdown fences.
- mistakes output must start with `mistakes:`.
- worksheet output must start with `worksheet:`.

Current v0.1.7.3 mistakes.yaml canonical structure:
```yaml
mistakes:
  - date: "2026-06-03"
    student_id: "daughter"
    subject_id: "math"
    grade_at_time: 6
    term_at_time: "六年级上"
    curriculum_version_at_time: "cn_k12_2022"
    textbook_version_at_time: "generic"
    question_type_code: "math_application"
    knowledge_point_id: "math_g6_application_modeling"
    primary_mistake_tag_code: "MATH_MODEL_1"
    difficulty_code: "medium"
    question_summary: "..."
    wrong_answer_summary: "..."
    correct_answer_summary: "..."
    training_needed: true
    source: "GPT marking"
```

Current v0.1.7.3 worksheet.yaml canonical structure:
```yaml
worksheet:
  title: "六年级数学专项训练"
  date: "2026-06-03"
  student_id: "daughter"
  subject_id: "math"
  grade_at_time: 6
  term_at_time: "六年级上"
  curriculum_version_at_time: "cn_k12_2022"
  textbook_version_at_time: "generic"
  sections:
    - name: "一、应用题建模"
      layout: "single_column"
      questions:
        - question_type_code: "math_application"
          knowledge_point_id: "math_g6_application_modeling"
          target_mistake_tag_code: "MATH_MODEL_1"
          difficulty_code: "medium"
          question: "..."
          answer: "..."
          explanation: "..."
```

original_yaml:
```text
{{ original_text }}
```
