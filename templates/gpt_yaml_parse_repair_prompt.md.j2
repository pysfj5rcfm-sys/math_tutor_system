Current YAML failed during parsing and has not reached field validation.

source_type: {{ source_type }}
error_type: {{ report.error_type }}
line: {{ report.line }}
column: {{ report.column }}
problem: {{ report.problem }}

human_message: {{ report.human_message }}

suggested_fix: {{ report.suggestion }}

Nearby lines:
```text
{% for row in report.nearby_lines -%}
{{ ">" if row.is_error_line else " " }} {{ "%4d"|format(row.line) }}: {{ row.text }}
{% endfor -%}
```

Repair only YAML syntax, indentation, list hierarchy, colons, and code fences.
Do not rewrite questions, answers, or explanations.
Do not output prose or Markdown fences.
Output must start directly with `{{ source_type_root }}:`.

Expected structure:
```yaml
{{ skeleton }}
```

Original YAML:
```text
{{ original_text }}
```
